Implement the core logic. Directives: {instruction}. Signatures: {function_signatures}. Strict type rules: {strict_type_validation_rules}. CRITICAL: DO NOT write any unit tests or test files. The QA node handles testing. Write ONLY production code. All files MUST be created preserving the exact directory structure specified in the contract, relative to {code_dir}. Example: if the contract says 'src/api/main.py', you must create '{code_dir}/src/api/main.py'.