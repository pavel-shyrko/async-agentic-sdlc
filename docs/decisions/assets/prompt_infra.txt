Act as a Principal System Architect and AI Engineer. Conduct an in-depth technical research on implementing a cost-effective, deterministic Agentic SDLC (Software Development Life Cycle) pipeline.

CONTEXT:
I am building an orchestration engine for a hackathon. The pipeline flows through 7 stages: Product → Planner → Architect → Developer → Reviewer → QA → DevOps. 
Constraint 1: High reliability (≥80% human-free execution). 
Constraint 2: Strict machine-readable contracts (JSON/Schema) between nodes.
Constraint 3: Cost-effective API routing (Gemini Pro for heavy context, Claude 3.5 Sonnet for complex coding).
Constraint 4: The Developer and QA nodes must execute in sandboxed environments, utilizing CLI-based worker agents (e.g., Aider, Claude Code) instead of bare LLM API calls.

RESEARCH OBJECTIVES:

1. ORCHESTRATION FRAMEWORKS (LIGHTWEIGHT DAG)
Compare "LangGraph (Python)" vs "Custom State Machine built on pure Python + Pydantic".
- How easily can each handle conditional cyclic routing (e.g., QA fails → return to Developer) with a strict "Circuit Breaker" (max 3 retries)?
- Provide a conceptual code snippet or architectural pattern for implementing a persistent state object that gets mutated as it passes through the DAG.

2. STRICT CONTRACT ENFORCEMENT & STRUCTURED OUTPUTS
I need to force Gemini and Claude APIs to output exact JSON matching complex Pydantic schemas.
- Research and compare "Instructor (Python)" vs "Native LLM Structured Outputs (Gemini JSON mode / Anthropic Tool Use)".
- Identify known failure modes when passing large ASTs (Abstract Syntax Trees) or code diffs within JSON payloads, and how to mitigate parsing crashes.

3. CLI WORKER INTEGRATION (SUBPROCESS WRAPPING)
The Developer node will trigger CLI tools (Aider or Claude Code). 
- Research the most robust Python patterns (`subprocess`, `pexpect`, or `asyncio.create_subprocess_shell`) to programmatically invoke these CLI tools, feed them a `TASK.md` generated by the Architect, and capture their standard output/error and exit codes.
- How to extract meaningful failure reasons from CLI stdout to pass back to the Reviewer/QA agents?

4. SANDBOXED EXECUTION (QA & DEVOPS)
Code generated by the Developer must be automatically tested.
- Research how to dynamically spin up isolated execution environments using Python (e.g., `docker-py` vs `Testcontainers`).
- Define a pattern where the QA Agent generates a test script, injects it into the Docker container alongside the source code, executes it, and retrieves the test results and logs for the DAG state.

5. CONTEXT PRUNING AND PROMPT CACHING (COST OPTIMIZATION)
- Investigate implementation details for Anthropic Prompt Caching and Gemini Context Caching in a cyclical DAG scenario.
- How to manage the "State/Memory" payload so that we don't send the entire project history on every loop iteration, but only the necessary diffs and failed test logs?

EXPECTED OUTPUT FORMAT:
- Provide a highly technical, dense report.
- Include comparison matrices for the tools evaluated (Frameworks, Schema Validators, Sandboxes).
- List critical architectural bottlenecks and potential points of failure (e.g., schema degradation, container zombie processes).
- Focus on practical, open-source, or standard library solutions to minimize dependencies and costs.